{
  "private": true,
  "scripts": {
    "prettier": "prettier --write './{apps,packages}/**/src/**/*.{ts,tsx}'",
    "prettier:check": "prettier --check './{apps,packages}/**/src/**/*.{ts,tsx}'",
    "start:services": "turbo run dev --filter=@example/*-service",
    "start:gateway": "turbo run dev --filter=@example/gateway",
    "codegen:services": "turbo run codegen:schema --filter=@example/*-service",
    "generate": "turbo run db:generate",
    "codegen": "turbo run codegen",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "packageManager": "pnpm@7.26.1",
  "resolutions": {
    "graphql": "16.6.0"
  },
  "devDependencies": {
    "@example/eslint-config-custom": "workspace:0.0.1",
    "@swc/cli": "0.1.62",
    "@swc/core": "1.3.35",
    "@types/node": "18.13.0",
    "eslint": "8.34.0",
    "prettier": "2.8.4",
    "turbo": "1.7.4",
    "typescript": "4.9.5"
  }
}
